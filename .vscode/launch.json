{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "MNWork: Démarrer le serveur",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/server.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
